<% if user_signed_in? %>

  <!-- # fix: Use Ruby 2 syntax for Hash -DONE --> 
  <%= link_to('Logout', destroy_user_session_path, method: :delete) %>

<% end %>
<%= link_to "Settings", '/settings/edit' %>
