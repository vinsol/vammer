<div class="list-group">
  <a href="#" class="list-group-item active">
    Groups
  </a>
  <% @my_groups.each do |group| %>
    <%= link_to group.name, :groups, class: 'list-group-item' %>
  <% end %>
  <%= link_to 'new group', :new_group, class: 'list-group-item' %>
</div>