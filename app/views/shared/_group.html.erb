<%= current_user.image ? image_tag(current_user.image.attachment.url) : missing_image_tag %>
<%= current_user.name.capitalize %>

<br> <br>
<div class='list-group'>

  <%= link_to 'Groups', groups_path, class: 'list-group-item active' %>

  <% @user_groups.each do |group| %>

    <%= link_to group.name.truncate(10), group, class: 'list-group-item' %>
    
  <% end %>

  <%= link_to 'New group', :new_group, class: 'list-group-item' %>
</div>