<div class='control-group col-md-offset-4'>

<!-- #FIX: Why using variable user for a post object? -->
<%= render partial: 'homes/error_message', locals: {errors: @post.errors} %>

<br><br>
</div>

<div class='form-group'>
<div class='col-lg-7 col-lg-offset-1'>
  <%= locals[:f].text_area :content, placeholder: 'what\'s on your mind', class: 'form-control' %>
</div>
<br><br><br><br>

<!-- #FIX: Use different views. Do not add conditions based on controller or action names -DONE -->

<div class='col-lg-offset-6'>

  <%= locals[:f].fields_for :documents do |attach| %>

    <%= attach.file_field :attachment %>

  <% end %>
  <%= locals[:f].link_to_add 'Add attachment', :documents %>

</div>
