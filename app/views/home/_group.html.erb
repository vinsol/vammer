<%= current_user.image ? image_tag(current_user.image.attachment.url) : image_tag('missing.jpg') %>
<%= current_user.name %>

<br> <br>
<div class='list-group'>

  <%= link_to 'Groups', groups_path, class: 'list-group-item active' %>

  <% @my_groups.each do |group| %>

    <%= link_to group.name, group, class: 'list-group-item' %>
    
  <% end %>

  <%= link_to 'new group', :new_group, class: 'list-group-item' %>
</div>