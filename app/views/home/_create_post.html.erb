<%= nested_form_for @post, class: 'form-horizontal' do |f| %>
	<%= render partial: 'shared/error_message', locals: {errors: @post.errors} %>
	<%= render 'shared/posts_form_fields', locals: {f: f} %>
<% end %>
